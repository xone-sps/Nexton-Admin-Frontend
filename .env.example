# Nexton Backend API base URL (no trailing slash). The client appends /api/v1.
# Dev:  http://localhost:8080   Prod: https://api.nexton.work
NEXT_PUBLIC_API_URL=http://localhost:8080

# Root domain used to build tenant subdomain URLs in the admin panel
NEXT_PUBLIC_TENANT_ROOT_DOMAIN=nexton.work
